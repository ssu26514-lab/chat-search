name: Build Card File Tools v1.2.1 APK

on:
  pull_request:
    branches:
      - main
    paths:
      - 'duplicate-cleaner/**'
      - '.github/workflows/build-card-tools-v121-pr.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 35
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        working-directory: duplicate-cleaner
        run: gradle :app:assembleDebug --stacktrace
      - name: Prepare artifact
        run: cp duplicate-cleaner/app/build/outputs/apk/debug/app-debug.apk card-file-tools-v1.2.1.apk
      - uses: actions/upload-artifact@v4
        with:
          name: card-file-tools-v121-apk
          path: card-file-tools-v1.2.1.apk
          if-no-files-found: error
